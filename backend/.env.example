DATABASE_URL=sqlite:///./payroll.db
# En producción (Supabase, connection string "Session pooler" o directa):
# DATABASE_URL=postgresql://postgres:PASSWORD@HOST:5432/postgres?sslmode=require
CORS_ORIGINS=http://localhost:3000
# En producción, lista separada por comas (sin espacios ni corchetes), ej:
# CORS_ORIGINS=https://payroll-creator.vercel.app,http://localhost:3000

JWT_SECRET=cambia-esto-por-un-valor-aleatorio-largo
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=720

ADMIN_DEFAULT_EMAIL=admin@example.com
ADMIN_DEFAULT_NOMBRE=Administrador
ADMIN_DEFAULT_PASSWORD=cambiar-esta-clave

# URL pública del frontend, para construir el enlace del correo de
# recuperación de contraseña.
FRONTEND_URL=http://localhost:3000

# SMTP para el correo de "recuperar contraseña". Con Gmail: crea una
# "contraseña de aplicación" en https://myaccount.google.com/apppasswords
# (requiere verificación en dos pasos activada) y úsala como SMTP_PASSWORD,
# NO tu contraseña normal de Gmail.
# Si dejas SMTP_USER/SMTP_PASSWORD vacíos, el enlace se registra en el log
# del servidor en vez de enviarse por correo (útil en desarrollo local).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true
RESET_PASSWORD_TOKEN_MINUTOS=30
