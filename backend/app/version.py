"""
Versión de la aplicación. Incrementar BUILD (y VERSION si el cambio es
significativo) en cada actualización que se despliegue a producción.
"""
VERSION = "1.0.0"
BUILD = "2749"
FULL_VERSION = f"{VERSION}+{BUILD}"
